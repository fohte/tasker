import { defineCloudflareConfig } from '@opennextjs/cloudflare'

export default defineCloudflareConfig({
  // Incremental cache configuration can be added here if needed
  // For now, we'll use the default configuration
})
